
This is an encoder and decoder written in C using Huffman Trees.

HOW TO USE:

compile using 

$ make all

encode a file using

$ encode -i infile -o outfile

decode a file using

$ decode -i outfile(from decode) -o finalOutFile
